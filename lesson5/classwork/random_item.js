let animals = ["cat","dog","rabbit","hamster","redpanda","monkey"];

let length = animals.length;

let random_index = Math.floor(ath.random() * length);
let random_animal = animals[random_index]
console.log("Random animal: ", random_animal)